# Copy to `.env` and fill in. `.env` is git-ignored and never committed.
# Only the LLM features (page-type fallback, irregular-page extraction) need this.
# The scraper runs without it — LLM features are simply skipped when absent.

# I'm using Claude as the llm.
ANTHROPIC_API_KEY=
